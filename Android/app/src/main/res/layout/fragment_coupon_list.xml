<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="#F5F5F5"
    android:padding="8dp">

    <!-- RecyclerView để hiển thị danh sách mã giảm giá -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewCoupons"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="80dp"
        android:clipToPadding="false"/>

    <!-- Nút FloatingActionButton để thêm mã giảm giá mới -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add_coupon"
        app:backgroundTint="@color/facebook_blue"
        app:tint="@android:color/white"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_margin="16dp"
        android:contentDescription="Add new coupon"
        android:src="@drawable/baseline_add_24" />
</RelativeLayout>
